<IfModule mod_rewrite.c>
  RewriteEngine On
  # RewriteBase /ecom/
  RewriteBase /

  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  RewriteRule ^ /ecom/index.html [L]
</IfModule>